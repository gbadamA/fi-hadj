# URL de l'API vue depuis le NAVIGATEUR (formulaires, back-office).
NEXT_PUBLIC_API_URL="http://localhost:3051"

# URL de l'API vue depuis le SERVEUR Next (rendu des pages publiques).
# Distincte de la précédente en production : le rendu serveur peut passer par le
# réseau interne, sans sortir par le nom de domaine public.
API_URL="http://localhost:3051"

# URL canonique du site — métadonnées Open Graph et sitemap.
NEXT_PUBLIC_SITE_URL="http://localhost:3050"
